# FPSRoguelite — 프로젝트 지침 (Codex/에이전트 자동 로드)

> **작업 시작 전 반드시 `Game.md`(SSOT 허브)와 Notion PM 보드(작업 현황 SSOT, `Docs/SSOT/Workflow.md` §6-9)를 먼저 볼 것.**
> 🔴 커밋을 낳는 작업은 **보드 클레임 없이 착수 금지**(하드 게이트, 2026-08-07). Notion 접근 수단이 없는 에이전트는 사용자에게 보고 후 대기. `PROGRESS.md`는 보드로 가는 포인터.
> 기획·설계·기술스택·코드구조·환경경로·빌드/검증법·모델정책은 `Game.md` 허브의 라우팅 가이드(§0-1)를 따라 **작업에 해당하는 `Docs/SSOT/*.md`** 만 읽는다(문서 분할, 섹션번호 보존).

## ⛔ 절대 금지 / 핵심 4원칙
1. **장르 정체성 = 제1원리** — 1인칭 FPS × 뱀파이어 서바이벌 × 4인 협동 로그라이트(레퍼런스 The Spell Brigade). **제1원리 = 적 수백(동시 ~200-300 실측, 캡 500) 싸게 → 액터당 비용 최소화 우선.** 적 스웜 = 경량 풀액터 + Flow-Field + Significance, 체력 = 경량 `UHealthComponent` + 비-GE 데미지. **GAS는 플레이어(1~4)와 보스/엘리트에만 붙인다.** (상세 Game.md §1)
2. **프로덕션 방식** — C++=로직/베이스, 콘텐츠=BP/DataAsset/config. 에셋 경로 C++ 하드코딩 금지. 서버권위+Push Model. 검증 없이 "완료" 보고 금지. (상세 §6 = `Docs/SSOT/Workflow.md`)
3. **핸드오프 = PM 보드(§6-9)** — 작업 전 보드 클레임, 완료·중단 전 보드 행 갱신(상태·로그·인계 컨텍스트). 우선순위: 크리티컬=사용자 전용 / 하이·미듐·로우=담당 판단 / 백로그=개선점. 설계 변경은 해당 `Docs/SSOT/` 도메인 파일 먼저. 새 작업은 플랜 우선, HIGH_RISK는 승인 후. **모델 배분 = 코어·구조설계·리팩토링 → 플랜·지시·검증 모두 Fable(구현만 Sonnet 위임, §6-5-2) / 그 외 → 구현=Sonnet 위임·검증=Opus 직접.** 검증·판정은 하위 모델 위임 금지. (상세 §6 = `Docs/SSOT/Workflow.md`)
4. **아키텍처 결정 = 제1원리 우선** — 구조 결정의 근거는 **제1원리(이 게임 제약·성능예산·서버권위/복제·다음 단계 확장성)**다. 비자명한 구조 결정은 플랜/보고에 **3줄로 명시**: ① 제1원리 근거 ② **엔진 기본값·기존 인프라와의 관계** — 그대로 쓰는지, 덮는지, 덮는다면 왜(엔진 기본값은 이 게임의 전제 위에 서 있지 않을 수 있다 — 소스로 확인할 것) ③ 이 프로젝트 제약과의 정합. 출시 코드 기준 = "최신"이 아니라 **검증됨 + 제약 적합 + 유지보수 가능**. 임시·미루기 구조 금지(프로덕션 구조 우선), 재작업 경량 지점만 "후속 단순확장"으로 명시.
