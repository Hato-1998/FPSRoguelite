# ArtDirection — 컬러 / 명도 · 채도 대역 (FPSRoguelite SSOT 분할)

> `Game.md`(SSOT 허브)의 분할 문서. 맵·에셋·VFX·UI 의 **색을 정하는 단일 기준**이다.
> 근거 = ① Riot 『The Complete Guide to Creating Visual Effects within League of Legends』(37p, VFX Discipline)
> ② 사용자 제시 회로기판 레퍼런스 이미지 ③ 이 프로젝트의 제1원리(`Game.md §1`)와 [ADR 0010](../Architecture/0010-arena-topology-and-stage-transition.md) D4·D8.
> 색을 새로 만들 때는 **§A-1 대역표를 먼저** 보고, 그 대역 안에서 §A-3 팔레트를 쓴다.

---

## A-0. 이 문서가 푸는 문제

레퍼런스 이미지를 **그대로 쓰면 게임이 안 읽힌다.** 두 가지가 겹쳐 있다.

**① 그 이미지는 히어로 아트지 플레이 가능한 환경이 아니다.**
이미지의 기판 부품은 채도·명도를 최대로 쓴다. 경쟁할 게임플레이 요소가 없으니 그래도 된다.
Riot 가이드의 골자는 정반대다 — **환경은 명도·채도 양쪽에서 가장 아래 대역을 쓰고**, 그 위의 헤드룸을
캐릭터 → VFX → UI 에 남긴다. 환경이 대역을 다 먹으면 그 위에 얹을 것이 없다.

**② 이미지의 마젠타가 하필 「적」 대역이다.**
가이드 p26 이 못박은 유일한 절대 규칙: **아군 = 청록→파랑→보라 / 적 = 분홍→빨강→주황.**
이 게임은 4인 협동 + 동시 200~300 적이다(`Game.md §1`). 배경이 마젠타면 스웜을 훑는 눈이 배경을 계속
적으로 오독한다 — 이 게임에서 가장 비싼 가독성 채널이 죽는다.

**해소** = **색상(hue)은 레퍼런스에서 가져오되, 명도·채도를 대역에 맞춰 다시 매핑한다.**
마젠타는 버리지 않고 **게임플레이와 무관한 자리로 옮긴다**(§A-3-6).

---

## A-1. 대역표 — 이 문서에서 가장 중요한 표

가이드 p13(명도)·p19(채도)의 구조를 이 게임에 옮긴 것이다. **순서 관계는 가이드 원문**이고,
**구체 수치는 이 프로젝트용 번역**이다(원문은 막대 그래프라 수치가 없다 — 수치는 PIE 로 조정 대상).

| 분야 | 명도 V | 채도 S | 비고 |
|---|---|---|---|
| **환경** (바닥·블로커·프롭·벽) | **6 – 40** | **15 – 40** | 가장 아래. 여기가 넓어지면 위가 전부 죽는다 |
| **L2 바닥 배선** | 35 – 60 | 45 – 70 | 환경이지만 "지나갈 수 있는 곳"이라 환경 상단 |
| **캐릭터** (플레이어 팔·무기·적 메시) | 25 – 70 | 30 – 60 | 환경 위, VFX 아래 |
| **VFX** | **15 – 95** | **20 – 90** | **가장 넓다.** 환경보다 어두워질 수도, UI보다 밝아질 수도 |
| **UI** | 65 – 95 | 35 – 80 | 가장 위. 스크린 공간이라 월드와 안 섞인다 |

**🔒 절대 규칙 3개 (가이드 p13 · p19 원문)**
1. **0% 도 100% 도 쓰지 않는다.** 순백·순흑·순색은 환경이나 UI 로 오독된다. VFX 도 예외 없다.
2. **명도 대역이 넓을수록 시선을 끈다.** 중요도가 낮은 것에 넓은 대역을 주지 마라.
3. **대비가 곧 주목 영역(area of focus)** 이다. 밝은 쪽·어두운 쪽 **양쪽 끝 다** 고대비 = 고주목(p17).

---

## A-2. 중요도 사다리 (가이드 p11 → 이 게임 매핑)

가이드는 VFX 의 세기를 **크기·모양·타이밍·명도·채도·불투명도**로 조절하고, 그 세기가
**게임플레이 영향력과 일치**해야 한다고 못박는다. 이 게임의 사다리:

| 가이드 등급 | 이 게임 | 명도 | 채도 | 불투명도 | 움직임 |
|---|---|---|---|---|---|
| Idle Particle | 바닥 배선 잔광 · 먼지 | 낮음 | 낮음 | 낮음 | 미세 |
| Basic Attack | 플레이어 사격 · 총구화염 | 중 | 중 | 중 | 짧고 빠름 |
| Defensive | 대시 · 슬라이드 · 실드 | 낮음 | **낮음** | 낮음 | 부드럽게 |
| Damage Spell | 적 공격 텔 · 피격 | 높음 | 높음 | 높음 | 선명 |
| **Game Changer** | **파괴물 파괴 · 그레이스 창 진입** | 높음 | 높음 | 높음 | 강함 |
| **Ultimate** | **억제기 파괴 → 스테이지 전환** | **최고** | **최고** | **최고** | 크고 임팩트 |

> 억제기가 이 사다리의 꼭대기라는 게 중요하다. ADR 0010 D6 이 스테이지 전환을 런의 최대 사건으로
> 잡았으므로, **억제기 파괴 연출이 다른 어떤 것보다 시각적으로 커야** 형태가 기능과 맞는다.

**1차 / 2차 요소 규칙 (p7)** — 모든 이펙트는 둘로 나눠 만든다.
- **1차**: 높은 명도 대역 · 뚜렷한 실루엣 · 강한 형태 · 높은 불투명도 · 강한 움직임 → **게임플레이를 전달**
- **2차**: 낮은 명도 대역 · 흐린 실루엣 · 단순 형태 · 작은 크기 · 낮은 불투명도 · 미세한 움직임 → **분위기만**

---

## A-3. 팔레트

⚠️ 아래 HEX 는 레퍼런스 이미지를 **눈으로 읽어 대역에 맞춰 재매핑한 값**이지 픽셀 샘플링 값이 아니다.
원본 파일 경로를 주면 정확히 샘플링해 갱신한다.

### A-3-1. substrate — 바닥·블록·벽의 복셀 면 (환경 · 가장 어둡고 가장 탁하게)

| HEX | 용도 |
|---|---|
| `#0A0912` | 보이드 / 배경 하늘 |
| `#151329` | 바닥 (어두운 면) |
| `#221E3D` | 바닥 (기본 면) |
| `#332B57` | 블로커·클러스터 측면 |
| `#41386B` | 블로커 상단 하이라이트 |

레퍼런스의 청보라 기조를 유지하되 채도를 내렸다. **이 다섯 색이 화면의 70% 이상**을 덮어야 위 대역이 산다.

### A-3-2. L2 바닥 배선 (통행 가능 = 밝은 라인, ADR 0010 D8)

| HEX | 용도 |
|---|---|
| `#1E5A66` | 비활성 / 먼 거리 배선 |
| `#2A8A96` | 기본 배선 |
| `#39B8B0` | 넓은 통로 배선 (`WidthClass` 높음 — **굵고 밝은 배선 = 넓은 통로**) |
| `#5FE0D2` | 정션 / 비아 (**동선이 갈라지는 지점**) |

배선은 환경 대역 상단이다 — **바닥보다 확실히 밝되 플레이어·적보다는 어둡다.** 이게 뒤집히면
"바닥 무늬가 캐릭터보다 눈에 띈다"가 되어 스웜을 못 읽는다.

### A-3-3. 파괴 가능 (ADR 0010 D4 세 번째 판단축)

D4 는 높이(넘을 수 있나) × 파괴가능 두 축을 **1인칭에서 즉시** 갈라야 한다고 못박았다.
높이는 형태로, **파괴 가능은 색 + 발광 펄스**로 가른다.

| HEX | 용도 |
|---|---|
| `#6FA82E` | 파괴 가능 코어 (원거리 / 약한 발광) |
| `#9BE33C` | 파괴 가능 코어 (기본) |
| `#C8FF5A` | 파괴 임계 직전 (`OnDestructibleDamageStage` 단계마다 이쪽으로) |

**왜 연두인가** — 적(뜨거운 쪽)과 아군(차가운 쪽) 양쪽에서 최대로 떨어진 유일한 색상대다.
호박색/주황은 관습적으로 "상호작용"이지만 **적 대역(분홍→빨강→주황)과 붙어** 스웜 스캔 중 오독된다.
연두는 이 게임에서 다른 용도가 없다. 부품이 **에너지를 띠면 부술 수 있다**는 기판 픽션과도 맞는다.

**억제기**는 같은 색을 쓰되 **더 크고 더 강하게 맥동**한다(§A-2 Ultimate). 색을 바꾸지 않는 이유 =
"연두 = 부술 수 있다"는 규칙이 하나여야 학습 비용이 안 늘어난다(가이드 p476 논지: 외우는 건 종류가 아니라 규칙).

### A-3-4. 적 — 뜨거운 쪽 (🔒 예약, 다른 용도 금지)

| HEX | 용도 |
|---|---|
| `#FF3B4E` | 적 실루엣 림 / 피격 |
| `#FF6B2C` | 적 공격 텔레그래프 |
| `#FF1E7A` | 엘리트 / 보스 |

### A-3-5. 플레이어 · 아군 — 차가운 쪽 (🔒 예약)

| HEX | 용도 |
|---|---|
| `#4FD8FF` | 아군 아웃라인 |
| `#2E9BFF` | 아군 인디케이터 / 핑 |
| `#8B6BFF` | 자기 자신 (아군과 구분) |

> 🔒 A-3-4 와 A-3-5 는 **가이드 p26 의 유일한 절대 규칙**이다. 환경·프롭·UI 장식 어디에도
> 이 두 대역의 **고채도·고명도** 색을 쓰지 않는다. 4인 협동 + 스웜에서 이게 무너지면 복구할 방법이 없다.

### A-3-6. 마젠타를 어디로 보냈나

레퍼런스의 상징색이라 버리지 않았다. 대신 **게임플레이와 무관한 자리로만** 보낸다:

- ✅ 아레나 **경계벽 바깥** 픽셀 패럴랙스 배경 — 스카이라인·부유 블록 실루엣 (플레이할 수 없는 영역 — 여기선 강하게 써도 된다. ADR 0016 D3)
- ✅ 바닥 실크스크린 · 각인 · 데칼 (**콜리전 없음 · ≤45cm 평면 장식**, 0010 D4)
- ✅ 먼 거리 배경 부품 덩어리
- ❌ 통행 판정이 걸린 모든 것(블로커·프롭·파괴물)
- ❌ 움직이는 모든 것
- ❌ 고채도 + 고명도 조합 (그 순간 적으로 읽힌다)

권장: `#5A2E63`(어두운 자두) ~ `#9B3F86`(경계 밖 강조). 플레이 공간 안에서는 앞쪽만.

### A-3-7. UI (스크린 공간 · 가장 위 대역)

| HEX | 용도 |
|---|---|
| `#EAF6FF` | 본문 텍스트 (primary) |
| `#8FA8C4` | 보조 텍스트 · 비활성 |
| `#5FE0D2` | 긍정 · 확인 · 진행 |
| `#FFC24A` | 보상 · 카드 · 하이라이트 |
| `#FF4D5E` | 경고 · 체력 위험 |

⚠️ `#FF4D5E`(UI 경고)와 `#FF3B4E`(적)는 색상이 가깝다. **UI 는 스크린 공간이고 명도 대역이 위**라
가이드 기준으로는 허용이지만, 화면 가장자리 경고 비네트처럼 **월드에 겹치는 UI 에는 쓰지 말 것**.

---

## A-4. 색상 관계 규칙 (가이드 p21)

- **유사색(analogous)이 기본이다.** 한 이펙트에 보색 둘을 쓰면 **둘이 1차 요소 자리를 놓고 싸운다** —
  명도 레벨이 달라도 싸운다.
- 보색을 쓸 거면 **한쪽은 반드시 2차 요소**로: 채도를 낮추고 불투명도를 낮춘다.
- 나쁜 예(원문): 보색을 고불투명·고채도로 겹쳐 노이즈만 생기고 초점이 사라짐.
- 좋은 예(원문): 밝은 노랑 1차 + **탈채도된 보라를 낮은 불투명도로** 2차 → 풍부해지되 안 싸움.

**이 게임 적용** — 기판 청보라(환경) ↔ 연두(파괴 가능)는 준보색이다. 그래서 파괴 가능은
**작고 발광하는 코어**로만 쓰고 면적을 주지 않는다. 면적을 주면 환경과 싸운다.

---

## A-5. 그 외 가이드에서 가져올 것

- **모델보다 VFX 가 명도·채도 범위가 넓다**(p20). 무기·적 메시를 화려하게 만들지 말고 **VFX 에 헤드룸을 남긴다.**
- **발광(illumination)이 마법성을 만든다**(p16). 단순한 글로우 하나로 대역이 넓어진다.
- **이펙트 중심의 명도를 밀어 올려라**(p15). 중심이 없으면 어디서 데미지가 나는지 안 읽힌다.
- **텍스처는 손그림 + 소프트/샤프 혼합**(p28). 사진 텍스처는 노이즈만 만든다. → 셀/툰 전환 방향과 정합.
- **타이밍**(p32~36): 모든 이펙트에 **예비동작(anticipation) → 본체 → 소산(dissipation)**.
  소산은 2차 요소로 취급해 명도·채도·불투명도를 낮춘다. **화면 잔류 시간은 의도적으로 짧게** — 스웜 게임에서 특히.
- **정확한 표현**(p8~9): 이펙트 크기 = 실제 판정 크기. 이 게임에선 폭발 반경·억제기 파괴 반경이 해당.

---

## A-6. 미해결 / 다음

- HEX 는 **레퍼런스를 눈으로 읽은 값**이다. 원본 이미지 파일 경로를 주면 샘플링해 갱신한다.
- 대역표의 수치는 **PIE 검증 대상**이다. 특히 배선(A-3-2)이 캐릭터보다 밝아지지 않는지 실측 필요.
- XP 픽업 색이 아직 이 표에 없다 — 현재 구현 색을 확인한 뒤 A-3 에 편입할 것.
- 랜드마크(`AFPSRArenaLandmark::BeaconColor`)는 **서로 확실히 다른 색 3개**가 필요하다(0011 E2).
  아군·적 대역을 피해야 하므로 후보 = 연두 계열은 파괴물이 먹었으니 **흰색 · 짙은 청록 · 자주** 조합으로 검토.
- ~~`Game.md` §0-1 라우팅 표 등록~~ ✅ 완료.

---

## B. 모티프 · 형태 · 픽셀 규칙 — 레트로 아케이드 픽셀 룩 ([ADR 0016](../Architecture/0016-art-direction-retro-arcade-pixel.md), 2026-09-06)

> §A 는 **색**이고 룩과 독립이다(모티프가 바뀌어도 살아남는 가독성 공학). §B 는 그 위에 얹는 **이 룩의 저작 규칙**이다.
> 시각 부록 = 컨셉 시트 아티팩트 「FPSRoguelite Arcade Pixel Concept Sheet」(아트보드 6장). 결정과 불변식의 정본은 ADR 0016 이고, 여기는 **저작할 때 보는 표**다.
> 키워드 = **FPS · 아케이드 · 픽셀 · 로그라이트.** 레퍼런스 = 롤 아케이드 스킨(컨셉·픽셀 요소·색감만) + 웹툰 『전자오락수호대』(게임 세계 수호 모티프). 캐릭터 디자인·의상·포즈·로고·서사는 참고하지 않는다.

### B-1. 픽셀은 세 층에 산다

| 층 | 무엇 | 어떻게 | 금지 |
|---|---|---|---|
| **형태** | 적 · 프롭 · 환경 블록 · 1P 무기 | **복셀**(3D 픽셀). 격자 = §B-3 | 매끈한 실루엣 + 표면만 계단(가짜 픽셀) |
| **평면** | VFX · 픽업 · UI 아이콘 · 데칼 | **픽셀 스프라이트** 8~16px 격자, 포인트 필터, 팔레트 제한, 알파 이분법 | 블러·그라데이션·부드러운 파티클 |
| **화면** | 전체 | **CRT 포스트프로세스** = `PP_Arcade` 현행값(스캔라인 270 · 강도 0.15 · 곡률 0 · 색수차 · 비네트) | **화면 전체 저해상도 픽셀화**(조준·원거리 판독을 깎는다 — ADR 0016 I6) |

### B-2. 레퍼런스 hue → 대역 재매핑 (§A-0 공식의 실행표)

레퍼런스가 배경 전면에 쓰는 색을 **어느 칸으로 보내는지**만 정한다. HEX 는 전부 §A-3 의 기존 값이다 — 새 색을 만들지 않는다.

| 레퍼런스 원색 | 이 게임에서 가는 곳 | 비고 |
|---|---|---|
| 주황 (배경·에너지) | 적 공격 텔레그래프 `#FF6B2C` · UI 보상/카드 `#FFC24A` | 🔒 A-3-4 적 대역. 환경 금지 |
| 마젠타 (픽셀 파티클) | 엘리트/보스 `#FF1E7A` · **경계벽 밖** 배경 `#9B3F86` · 바닥 실크스크린 `#5A2E63` | A-3-6. 플레이 공간 금지 |
| 연두 (LED·에너지) | 파괴 가능 코어 `#9BE33C` → 임계 `#C8FF5A` | 작게·발광만. 면적 금지 |
| 시안 (전자·홀로) | 아군 아웃라인 `#4FD8FF` · 넓은 통로 배선 `#39B8B0` · 정션/UI 긍정 `#5FE0D2` | 🔒 A-3-5 |
| 보라 (하늘·그림자) | substrate `#221E3D`·`#332B57`(화면 70%) · 경계 밖 스카이라인 `#5A2E63` · 자기 자신 `#8B6BFF` | 환경의 본체 |
| 금색 (코인·별) | 코인·별·카드 픽업 `#FFC24A` | 월드에선 8px 스프라이트 크기 |

**면적 예산**: substrate 70%+ / 배선·적 몸통 ~20% / 팝(주황·금·연두·마젠타) **합계 10% 미만**. 팝은 자리로 낸다(픽업·코어·UI·경계 밖), 면적으로 내지 않는다(사용자 결정 2026-09-06 "어두운 무대 + 팝 액센트").

### B-3. 복셀 격자 계층 (제안 초기값 — PIE 육안 조정 대상)

| 클래스 | 격자 | 근거 |
|---|---|---|
| 환경(블로커·벽·프롭) | **5 cm** | 콜리전 셀 100 · 문턱 45 · 60 이 전부 정수배(20 · 9 · 12). 면은 평평, 격자는 머티리얼 선 |
| 적 · 픽업 | **7.5 cm** | 드론 = 16×16×6 칸(120×120×45cm, `gen_voxel_drone.py` 층 기술 = 정본, 시트 3뷰는 파생). 구 쩝쩝이 18층·135cm 는 대체됨(2026-09-06) |
| **3P 플레이어 캐릭터** | **3.75 cm** | 적 격자의 정확히 1/2 = 아군이 2배 세밀하게 읽힌다 · 병목은 사지 굵기라 맨팔 2칸(7.5cm)이 하한 · 161.25cm = 43층. 스펙 정본 = `Docs/PlayerCharacter_ResumePrompt.md`(2026-09-08 NEON-V 철회 이후 재설계, 격자·수치는 무변) |
| 1P 무기 | **2.5 cm** | 카메라 30~60cm 앞 — 화면 픽셀 크기가 적과 비슷해진다 |

- **한 오브젝트 한 격자**(믹셀 금지, ADR 0016 I4). 클래스 사이 차이는 거리가 보정한다.
- 환경 격자는 **렌더 전용**이다 — 콜리전·마스크·플로우필드 베이크 무영향(0012 불변식 2). 45~60cm 저작 금지 대역은 그대로.
- 복셀 메시 폴리 예산은 격자가 아니라 **면 병합(greedy meshing)** 이 정한다. 적 300 에서 병합 자격(0007) 실측 뒤 생성기에 넣는다.
- 캐릭터 격자는 **콜리전과 무관**(판정은 캡슐이 진다) · **3P 전용** — 1P 는 총만 보인다(ADR 0015).

### B-4. 면은 안, 패럴랙스는 밖 (정정 2026-09-06(b) — Tron 네온선·와이어프레임·회로기판 픽션 전면 폐기)

| 영역 | 언어 | 구성 |
|---|---|---|
| **플레이 공간**(콜리전 있음) | **복셀 면** | §A-3-1 5색 평면 채색 + 같은 대역 안의 어두운 격자선(복셀 경계, 명도차 작게). 밝은 선은 **바닥 배선**(§A-3-2)만 — 통행 판독 규칙이라 유지(사용자 2026-09-06) |
| **경계벽 밖**(플레이 불가) | **픽셀 패럴랙스 배경** | 레트로 게임 배경 레이어 — 픽셀 별(2px) · 먼 스카이라인 계단 실루엣(8px 격자) · 부유 픽셀 블록. **전부 단색 실루엣, 선·와이어프레임·회로 없음.** 마젠타 계열은 여기서만(§A-3-6). 구역마다 레이어(높이·밀도·색)를 바꿔 무드 변주 |
| 경계벽·블록 격자선 | 복셀 면 + **저명도 격자선**(정정(d) 복귀) | `#221E3D` 면 · `#41386B` 엣지. 격자선 = 청·보라 계열, **환경 상단 대역 이하**(통로 배선보다 어둡게), 1m 눈금 굵게·5cm 눈금 가늘게. 마젠타 선은 저채도 자두. 통로 배선만 밝다 |

밋밋 방지 5레버 — ① 구역 색 ② 밝기 대비 ③ 밀도 변주 ④ 배선 발광 ⑤ 실루엣. 패럴랙스 레이어가 ①③⑤를 진다. 3구역 예 = 컨셉 시트 「적 라인업 · 3구역」.

### B-5. 실루엣 · 텍셀

- **실루엣 패밀리 3종** — 십자(쿼드 드론 · 기본 일반, 2026-09-06 구 쩝쩝이 대체) / 각(비틀·탱커 · 낮고 넓음) / 침(글리치 · 빠르고 얇음). 덩어리가 먼저, 디테일은 **복셀 1칸** 단위로만.
- **읽힘점 = 발광 코어 1개**(공격 텔레그래프 색 `#FF6B2C`). 엘리트 = 같은 실루엣 × 1.3 + 림 `#FF1E7A` + 코어 2개(ADR 0013 티어를 형태로).
- **텍스처 없음.** 색 = 요소ID LUT(`floor(UV.u)`, 12요소 계약 — 드론 = `gen_voxel_drone.py` 헤더, 로터 허브는 `floor(UV.v)`) 또는 정점색 + 격자선 + 이미시브. "복셀 1칸 = 색 1개." 사진·노이즈 텍스처 금지(§A-5).
- 무기 악센트 = **아군 대역 예외**(ADR 0016 I8): 1P `#8B6BFF` · 3P 팀원 무기 = `#4FD8FF` 계열 저채도. 월드에 놓인 무기는 프롭 규칙(`#FFC24A`).

### B-6. 모티프 → 게임플레이 매핑

| 모티프(픽셀 스프라이트) | 게임플레이 | 대역 |
|---|---|---|
| 코인 | XP · 재화 픽업, 자석 흡수 | 픽업(VFX) `#FFC24A` |
| 별 ★1~3 | 카드 희귀도 | UI |
| 1UP 하트 | DBNO 부활 완료 · 회복 픽업 | 차가운 쪽 `#5FE0D2` |
| 파워업 카드 | 레벨업 카드(금색 테두리) | UI |
| 캐비닛 | 억제기 = 스테이지 보스 코어. 화면이 얼굴 | 연두 코어 + substrate |
| 글리치 블록 | 적 스폰 지점 · 버그 픽션 | 자두 `#9B3F86`(경계 밖 대역) |
| 조이스틱 | 핑 · 상호작용 프롬프트 | UI |
| 크로스헤어 | U12 절차 SDF 유지 + 픽셀 스냅 | UI |

**어휘 번역**(문구 = LOC0 String Table 콘텐츠): 런 = 1코인 플레이 · 레벨업 카드 = 파워업 · 스테이지 전환 = **STAGE CLEAR**(§A-2 Ultimate) · DBNO = **CONTINUE? 9→0**(동료가 오면 정지) · 로비 = INSERT COIN · 런 종료 = GAME OVER + 하이스코어.
**픽션**: 게임 세계 다이브(`Concept.md §1-C-9`) + 수호자 — 플레이어 4인 = 접속한 히어로(차가운 색), 적 = 세계를 갉아먹는 버그·글리치(뜨거운 색).

### B-7. 폰트 · UI 스킨 (✅ 확정 2026-09-07, HUD 재스킨 세션)

**폰트 = Galmuri11 (OFL)** — 사용자 결정 2026-09-07. 후보였던 "Press Start 2P + Galmuri" 조합은 **기각**: Press Start 2P 는
한글 글리프가 **0자**(실측)라 반드시 2폰트 폴백이 되고 가로폭이 커서 좌하 `150 / 150` 이 칸에 안 들어간다.
Galmuri 는 라틴·숫자·한글을 **한 가족**으로 덮으므로 이음매가 없다(실측: 한글 음절 11,172/11,172 · 자모 51 · `★ ☆ — · … ×` 포함).

| 에셋 | 값 |
|---|---|
| `Content/Assets/Font/Galmuri11` · `Galmuri11-Bold` | FontFace. **`Hinting = Monochrome`** — 안티에일리어싱을 꺼야 픽셀이 안 뭉갠다 |
| `Content/Assets/Font/F_Galmuri` | Font 에셋 1개에 타입페이스 둘(`Regular`/`Bold`). Runtime 캐시 |
| `Docs/Licenses/Galmuri-OFL-1.1.txt` | OFL 1.1 원문 + 출처 |

- **크기는 설계 11px 의 정배수만**(11 / 22 / 33). 비정수 배율은 픽셀 격자를 깬다.
- **굵기로 위계를 낸다** — 본문·값 = Bold / 보조·비활성 = Regular. §A-3-7 의 본문 `#EAF6FF` ↔ 보조 `#8FA8C4` 와 축이 겹쳐 서로를 보강한다.
- 리포에 이미 있던 `DNFBitBitv2`(데미지 넘버·설정 위젯 사용 중)는 **HUD 에 쓰지 않는다** — 커버리지는 완전하지만 모서리가 둥근 벡터라
  픽셀 격자가 없고 굵기가 한 종뿐이다. 메뉴·로비 재작업 행에서 한 번에 정합을 맞춘다.

**텍스트 스타일 SSOT = `UCommonTextStyle` BP 8종** (`Content/UI/Style/`). CommonUI 는 이미 ON 이고 `UFPSRRunHUDWidget` 이
`UCommonUserWidget` 이라 새 인프라가 아니다. ⚠️ `UCommonTextBlock::UpdateFromStyle()` 이 `SetColorAndOpacity(TextStyle->Color)` 로
**색까지 강제**하므로(엔진 소스 확인), 스타일은 "크기 3종"이 아니라 **의미 역할별**이어야 SSOT 가 성립한다 — 각 스타일이 §A-3-7 색 하나에 1:1.

| 스타일 | 크기·굵기 | 색 (§A-3-7) | 쓰임 |
|---|---|---|---|
| `TS_HUD_Label` | 11 Bold | `#EAF6FF` | 패널 라벨·제목 |
| `TS_HUD_LabelDim` | 11 Regular | `#8FA8C4` | 보조·비활성·잠김 |
| `TS_HUD_Value` | 11 Bold | `#EAF6FF` | 값 숫자 |
| `TS_HUD_Positive` | 11 Bold | `#5FE0D2` | 긍정·진행·실드 |
| `TS_HUD_Reward` | 11 Bold | `#FFC24A` | 보상·XP·카드 |
| `TS_HUD_Warn` | 11 Bold | `#FF4D5E` | 경고·체력 위험 |
| `TS_HUD_Title` | 22 Bold | `#EAF6FF` | 배너·스테이지명 |
| `TS_HUD_Big` | 33 Bold | `#EAF6FF` | 큰 숫자(탄창·LV·스테이지) |

- HEX 는 **sRGB 로 읽어 리니어로 변환**해 넣는다(§A-3-7 규약). 전 스타일에 **1px 하드 그림자**(오프셋 (1,1), 검정 α0.75) —
  픽셀 UI 관례이자 §2-14 의 「적 200 + VFX 위에서 읽혀야 한다」 요구.

**부품 = 세그먼트 바 1종이 모든 바를 그린다.** `WBP_PixelSegBar` + `M_HUDSegBar`(UI 도메인, **텍스처 0장**).
파라미터 `SegmentCount`/`Percent`/`GapFrac`/`FilledColor`/`EmptyColor` → HP·실드 10칸 / XP 12칸 / 보스 진행 24칸을 같은 부품으로.
꺼진 칸 = 보조색 `#8FA8C4` α0.22. 채움 개수 = `ceil(Percent × SegmentCount)`(살아 있으면 최소 1칸이 남는다).
⚠️ 칸을 이미지 N개로 만들지 말 것 — 24칸 × 4바를 Tick 마다 도는 ForEach 는 제1원리(액터당 비용 최소화)에 어긋난다. 세터 1회·드로우콜 1개가 맞다.

**카드 희귀도 = ★1~4** (사용자 결정 2026-09-07 — `ECardRarity` 가 Common/Rare/Epic/Legendary **4단계**라 ★1~3 은 매핑이 안 맞는다).
테두리 색 사다리 = Common `#8FA8C4` → Rare `#2E9BFF` → Epic `#8B6BFF` → Legendary `#FFC24A`.
전부 기존 팔레트(§A-3-7 + §A-3-5 아군 차가운 쪽 변형) 안이고, **빨강은 적 대역이라 희귀도에 쓰지 않는다.**

- HUD 위젯은 세터·값 소스·가시성 규칙 유지, **스킨만** 교체 — 세그먼트 바, 픽셀 아이콘, 탄약 큰 숫자.
  월드에 겹치는 UI(적 체력바·비네트)에는 뜨거운 색 금지(§A-3-7 경고 그대로).
- **Synty `UISciFiSoldierHUD` 팩 위젯은 더 쓰지 않는다**(사용자 결정 2026-09-07) — 아트 컨셉 확정 전에 붙인 것이고
  아케이드 픽셀과 언어가 다르다. `_Clean` 무기 아이콘 텍스처만 재사용.

### B-9. 맵 저작 규칙 — 팩맨 미로 + 구석 특수 에리어 (ADR 0016 D9, 2026-09-06)

| 요소 | 규칙 |
|---|---|
| 통로 | 1타일 폭. 바닥 = `#151329` + 가운데 배선 1줄(§A-3-2, 정션 = 비아 `#5FE0D2`). 워프 터널 배선은 `#1E5A66`(먼 거리 톤) |
| 벽 | 복셀 면 `#332B57` + 상단 엣지 `#41386B` 1줄. 발광 테두리 금지. 격자선은 5cm 환경 격자(머티리얼) |
| 중앙 고스트 하우스 | 캐비닛 코어(억제기) + 글리치 스폰 블록. 코어 = 연두 §A-3-3(Ultimate 자리), 스폰 블록 = 자두 `#9B3F86` |
| 구석 특수 에리어 ×4 | 구석 블록 3×3 을 파낸 방. 바닥 = substrate 안의 방별 고유 틴트(예 `#1F1A3A` `#171F36` `#241D2E` `#1D1F38`), 프레임·아이콘만 UI 대역(보상 = 금 `#FFC24A` / 회복·아군 계열 = `#5FE0D2`). 내용(후보 A 파워업 부스 · B CONTINUE 부스 · C 보너스 존 · D 무기 캐비닛)은 **사용자 결정** |
| 시작점 | 팩맨 시작 위치(하단 중앙). 아군 색 마커 |

⚠️ 타일 크기(5 m vs 10 m)·위상 전환·베이크는 **ADR 0017 대상** — 여기는 색·형태만.

**✅ 결정(2026-09-06 사용자)**: 타일 = **10 m 유지**(280 × 310 m — ADR 0012 규모 상한 개정 + 플로우필드 예산 실측이 ADR 0017 스코프에 들어간다). 구석 특수 에리어 = **C 보너스 존**(기존 미션 HoldZone·CollectOrbs 가 붙는 자리)만 채택, A·B·D 는 후보로 보류. **안전지대 아님** — 적은 들어오고 보상만 방 안에서. 네 구석을 전부 C 변형으로 쓸지는 ADR 0017 에서.

### B-10. 방향성 기준 키아트 번역표 (ADR 0016 D10, 2026-09-06(d))

기준 이미지 = `Docs/Concept/ArcadePixel_KeyArt_UserRef_2026-09-06_v2_HUD.png`(사용자 최종안, HUD 배치 포함; v1 은 기록). **복제가 아니라 번역** — 아래 표가 저작 시 판정 기준.

| 이미지 | 저작 규칙 |
|---|---|
| 벽·블록 네온 격자선 | 허용. 청·보라 계열 저명도(배선보다 어둡게). 마젠타 선 = 자두 `#5A2E63`. 1m 눈금 굵게, 5cm 눈금 가늘게 |
| 통로 중앙 밝은 라인 | 배선 `#39B8B0` / 비아 `#5FE0D2`. 이미지의 핑크 짝 라인은 적 대역 → 보라 `#8B6BFF` 저명도 또는 생략 |
| 별·성운 하늘 | 픽셀 패럴랙스(B-4). 성운 = 자두 단색 실루엣 층 |
| 쿼드 드론 적 | 형태 채택, 색은 뜨거운 쪽만(`#FF3B4E` 라이트 · `#FF6B2C` 텔레그래프 · `#FF1E7A` 엘리트). **시안 금지.** 몸통 = 캐릭터 대역 어두운 자주(`#3A2748`·`#2A1E36`·`#6E2E44`). 실행 = `Docs/DroneEnemy_ResumePrompt.md` |
| 복셀 권총(시안+핑크) | 시안·보라 악센트만(B-5 무기 예외). 핑크 → `#8B6BFF` |
| HUD: HIGH SCORE · KILL · COMBO · 레벨명 배너 | B-6 어휘에 추가. 스크린 공간(§A-3-7). 콤보 색 = 보상 금 `#FFC24A`, 레벨명 배너 = 본문 `#EAF6FF` |

**보조 레퍼런스 2장(같은 구도, 2026-09-06 사용자)** — 기준은 여전히 v2. 아래는 각 장에서 **가져올 것 / 버릴 것**.

| 이미지 | 가져온다 | 버린다(규칙 충돌) |
|---|---|---|
| `..._v3a_dense.png` (빽빽·고채도) | ① **픽셀 간판·슬로건**("OBEY THE CUBE" 류) = 벽 장식 모티프 — 게임 세계 픽션(다이브·수호자)과 정합. 단 **벽면 텍스트는 환경 대역 저명도**, 밝은 간판은 경계 밖 패럴랙스 층에만 ② **지상 탱크형 적** = 각(BLOCK) 실루엣 패밀리 후보 ③ **드론·탱크의 빨간 라이트** = 뜨거운 쪽 규칙의 정답 예시(드론 트랙 참조) ④ 보스 진행 바의 **해골 마커** ⑤ MAIN SKILL 4슬롯 + 잠금 = 해금 진행 표현 ⑥ 부유 큐브 보스 = 캐비닛 코어의 대안 형태(결정 대상) | 전면 고채도 마젠타·시안(팝 10% 상한 위반) · 총기 빨간 디스플레이(적 대역 → 보라) · HP 녹색 · 팀원 바 녹/시안/빨강(빨강 = 적 대역) |
| `..._v3b_minimal.png` (어둡고 성김) | ① DASH **쿨다운 링 + 잔여초** ② 진행 바 **마커 아이콘 + %** ③ WEAPON 패널 헤더·큰 아이콘 ④ 팀원 초상 **플레이어별 프레임 색** — 단 색은 **차가운 쪽 변형**(시안 `#4FD8FF` · 파랑 `#2E9BFF` · 보라 `#8B6BFF` · 청록 `#39B8B0`)으로, 이미지의 빨강·노랑은 금지 ⑤ 어두운 무대의 밀도 하한 예시 | 환경이 **순수 와이어프레임 선**(정정(b) 폐기 방향) — 벽 격자선은 저명도로만 · MAIN SKILL 공란 |

두 장은 밀도의 양 끝이다 — **v3b(하한) ≤ 목표(v2) ≤ v3a(상한)**. 구역 무드 변주(B-4 5레버)는 이 사이에서 움직인다.

### B-11. HUD 배치 최종안 ↔ 시스템 대조 (사용자 최종안 2026-09-06(e), `Docs/Concept/ArcadePixel_KeyArt_UserRef_2026-09-06_v2_HUD.png`)

배치 자체는 현행 위젯 자리와 맞는다 — 좌상 팀/아이템 · 좌하 바이탈 · 우하 무기 · 상단 중앙 진행 · 우상 스테이지 · 하단 중앙 배너. 요소별 데이터 소스와 상태:

| 이미지 요소 | 데이터 소스(현행) | 상태 | 저작 시 규칙 |
|---|---|---|---|
| COLLECTED ITEMS 아이콘 그리드 | 획득 카드 원장 `FFPSRAcquiredCard` + `OnAcquiredCardsChanged`(PlayerState, CRIT2) — 둘 다 BlueprintPure/BlueprintAssignable 로 이미 열려 있다(실측 2026-09-07) | 데이터 ✅ · 위젯 ❌ | ⚠️ **카드 DA 에 아이콘 필드가 없다**(`Source/.../Card/*.h` grep 확인) → 1차는 **희귀도 ★1~4 + 테두리 색**만(§B-7). 카드별 고유 픽셀 아이콘은 C++ 필드 추가 + CSV 임포터 정합이 필요한 **후속 행** |
| 팀원 초상(픽셀) | — | ❌(3P 캐릭터 미정 — `Docs/PlayerCharacter_ResumePrompt.md` 트랙) | 초상 = 8×8/16×16 픽셀, 아군 색 테두리 |
| TEAM STATUS 패널 ×3 | `AFPSRGameState::GetPartyVitals()` · `WBP_TeammateVitals` | ✅ | **패널 1개 = 팀원 1명**(이미지의 P1/P2/P3 반복은 오류). 자기 자신은 좌하 바이탈. 다운 = `#FF4D5E` 경고 |
| STAGE PROGRESS TO BOSS 바 | 런 디렉터 BossTime(`UFPSRRunScheduleDataAsset`) + `GetRunClockSeconds/GetRunPhase` | ✅(산출 필요) | 정의 = **보스타임까지 시간 진행률**. 억제기 파괴(스테이지 전환)와 별개 축. 색 = 긍정 `#5FE0D2` |
| CURRENT STAGE 5-1 | `AFPSRGameState::GetStageIndex()` + 서브레벨(Map_1/Map_2/Boss) | ✅ | "5-1" = 스테이지 5 · 맵 1. 문자열 = LOC0 |
| XP BAR · LV 12/15 | `GetPartyLevel · GetSharedXP · GetRequiredXPForNextLevel · GetXPProgress01`(RunHUD) | ✅ | 레벨 상한 없음 → "LV 12" + 바 또는 XP/필요XP. 색 = 보상 금 `#FFC24A` |
| HP 150/150 · SHIELD 50/50 | VIT1 2층 · `WBP_PlayerVitals` | ✅ | ⚠️ **HP 녹색 금지**(연두 = 파괴물 예약 §A-3-3) → HP = 본문 `#EAF6FF` 세그먼트, 위험 시 `#FF4D5E`. 실드 파랑 `#2E9BFF` OK |
| MAIN SKILL 슬롯 | 플레이어 GAS 있음 · 액티브 스킬 정의 없음 | ✅ 결정(2026-09-06): **슬롯만 그려 둔다**(4칸, 잠금 표시) | 기능 = 별도 스킬 설계 행 |
| DASH 12s 쿨다운 → **플레이어 스킬 칸** | — | ✅ 결정(2026-09-06): **대시는 만들지 않는다.** 하단 중앙 육각 칸은 **플레이어 스킬이 들어갈 자리**로 HUD 배치만 유지 | 자리만. 기능은 스킬 설계 후 |
| LEVEL 5 - CORES OF THE VOID 배너 | `WBP_MissionBanner`(미션 배너) 재사용 · 스테이지명 데이터 없음 | 위젯 ✅ · 데이터 ❌ | 스테이지명 = LOC0 문자열 테이블 신규. 색 = 본문 |
| AMMO 2 · RESERVE 2400 · 무기 아이콘 | `WBP_WeaponPanel`(`GetCurrentAmmo/GetCurrentMagSize/Icon`) | 탄창 ✅ · ✅ 결정(2026-09-06): **예비탄 없음, 탄약 무한** | HUD 는 탄창 숫자만. RESERVE 줄 삭제 |
| 크로스헤어 | U12 절차 SDF | 이미지에 없음 — **필수 유지** | 픽셀 스냅만 |
| 코어(억제기) 방향 마커 | `AFPSRArenaLandmark`·억제기 | 이미지에 없음 — 팩맨 미로에서 **필요** | 연두 마커 + 거리(이전 시트 HUD 모크 참조) |
| 한글 표기("경험치IENCE BAR" 등) | — | 이미지 생성 오류 | ✅ 확정 = **Galmuri11**(§B-7). 라벨은 LOC0 String Table 키로 — 하드코딩 문자열 0 |

**표면 실측 (2026-09-07) — 이 표의 "✅(산출 필요)" 3건은 전부 C++ 신규 0줄로 해결된다:**

| 필요한 것 | 이미 있는 표면 |
|---|---|
| 보스 진행률 | `AFPSRGameState::GetRunTotalDuration()` **BlueprintPure** — 주석이 *"The timeline bar fills GetRunClockSeconds()/this and the boss icon sits at the end"* 라고 이 바를 위해 만들었다고 명시(`FPSRGameState.h:299`) |
| 스테이지 `n-m` | `GetStageIndex()` · `GetActiveArena()` BlueprintPure + 아레나의 `StageOrder`·`ArenaRole` BlueprintReadOnly. 갱신 훅 = `OnStageTransitionChanged`(BlueprintAssignable) 라 Tick 불요 |
| 카드 원장 | `GetAcquiredCards()` BlueprintPure + `OnAcquiredCardsChanged` BlueprintAssignable |

셋 다 `OnRep_StageTransition`/`OnRep_RunState` 로 **복제**되므로 4인 원격 클라에서도 산다.

### B-12. 조명 — 네온은 장식이 아니라 광원이다 (사용자 GI 레퍼런스 2026-09-06(g), ADR 0016 D11)

v3a·v3b 는 **Global Illumination 이 켜진 렌더**다. 참고점은 구도가 아니라 **빛**이다: 이미시브 선·패널이 바닥과 벽에 색을 튀기고, 바닥이 그 색을 낮게 반사한다. 이것이 "어두운 무대 + 팝 액센트"를 밋밋하지 않게 만드는 다섯 번째 레버(B-4 ④ 배선 발광)의 실체다.

| 규칙 | 내용 |
|---|---|
| **표면 색은 평면, 조명은 그라데이션** | I7(텍스처 없음·표면 그라데이션 금지)은 **알베도**에 대한 규칙이다. GI 바운스·반사가 만드는 밝기 변화는 조명이므로 허용 — 오히려 필요하다 |
| **광원 = 이미시브 + 소수의 실제 라이트** | 5cm 격자선처럼 얇은 이미시브는 GI 에 거의 기여하지 않는다(표면 캐시 해상도). 색을 튀길 곳(통로 배선·코어·간판 패널)은 **면적이 있는 이미시브 패널**로 만들거나 **로컬 라이트 소수**(구역당 손에 꼽게)를 둔다 |
| **바닥 반사 = 낮게** | 러프니스 0.5~0.7 대의 흐린 반사. 거울 반사는 원거리 적 판독을 깬다(반사된 적 코어가 진짜 적으로 읽힘). 반사 안의 뜨거운 색은 실제 적보다 항상 어둡다 |
| **바운스 색도 대역 규칙 안** | 바닥에 튄 마젠타·시안 바운스는 환경 상단 대역(V ≤ 60)을 넘지 않는다. 노출은 MANUAL 고정(`ArcadePostProcess_ResumePrompt.md §2-1`, 4인 화면 출렁임 방지) |
| **적 코어 발광은 GI 기여 금지** | 적 200~300 의 코어가 바닥에 빛을 튀기면 화면이 뜨거운 색으로 물든다. 적 이미시브는 **화면에만**(GI 표면 캐시 제외 = 메시 `Affect Dynamic Indirect Lighting` 끄기) |
| **방식 = Lumen 전제 + 실측 게이트**(사용자 2026-09-06) | Lumen GI·Lumen 반사를 전제로 저작한다. 적 300 스웜 + 4인 실측에서 `Performance.md` 예산 초과 시 화면공간(SSGI·SSR)으로 내린다. 에디터 프로젝트 세팅 현재값 확인은 실측 행에서 |

### B-13. 피부 대역 (플레이어 3P 캐릭터, 사용자 결정 2026-09-07 · 2026-09-08 적용 범위 갱신)

캐릭터 노출부(허벅지·복부·팔의 맨살)는 색상(hue)이 따뜻한 계열이라 §A-3-4 적 예약 대역과 겹칠 위험이 있다. 4인 협동 × 적 200~300 화면에서 이게 무너지면 ADR 0016 I1(룩과 독립인 절대 규칙)을 어기고, 이 등급은 복구 불가다.

- **규칙 = 살색은 §A-1 캐릭터 대역 하단(V 45~55 / S 12~20)의 저채도 뉴트럴.** 적 `#FF3B4E`(S 77)와 채도 4배 격차를 확보한다.
  ⚠️ 이 채도 하한(S 12)은 §A-1 캐릭터 대역의 채도 하한(S 30)보다 낮다 — **의도된 예외다**(저채도화 자체가 목적이므로, 캐릭터 대역 일반 규칙보다 이 항목이 우선).
- **판독 책임은 살색이 아니라 아군 아웃라인 `#4FD8FF`가 진다.** 살색으로 아군을 식별하게 만들지 않는다 — 살색은 어디까지나 중립화된 배경 색일 뿐이다.
- 노출 면적·실루엣은 디자인 자유(사용자 결정 2026-09-07) — 이 규칙이 거는 제약은 **색뿐**이다.
- 적용 대상 = 플레이어 3P 캐릭터 · 로비 전시 · UI 초상. **적에는 적용하지 않는다**(적은 §A-3-4 그대로).
- (2026-09-08 설계 전환 — 의상이 다리를 발목까지 덮게 되어 노출부가 얼굴·손뿐이다. 이 규칙은 존치하되 적용 면적이 크게 줄었다.)

### B-8. 미해결 / 다음

- 격자 수치(5/7.5/2.5)·팝 면적 10%·환경 대역 상단은 **PIE 육안 뒤 조정**. 안 B(중간 밝기)는 열어 둔다(ADR 0016).
- **2026-09-06 사용자 결정 6건** = ADR 0016 「사용자 결정 기록」 절. 맵 타일 10m(ADR 0017 = 0012 규모 개정 + 플로우필드 예산 실측) · 구석 = C 보너스 존, 안전지대 아님 · 드론 = 지상·돌진→초근접 사격·유령 대체 · HUD = 대시 없음(스킬 칸 자리만)·스킬 슬롯만·예비탄 없음 · 조명 = Lumen 전제 + 실측.
- ~~쩝쩝이 몸통 색 확정~~ → **쩝쩝이는 복셀 드론으로 대체**(사용자 2026-09-06, `Docs/DroneEnemy_ResumePrompt.md`). 드론 라이트·코어 = A-3-4 안에서.
- **ADR 0017 아레나 = 팩맨 미로**(타일 크기 · 0010 D1 위상 대체 · 0012 규모 상한 · 구석 특수 에리어 4곳 내용) — 사용자 결정 대기.
- 아레나 블록 복셀 면 프로토(5cm 격자 머티리얼) · 복셀 무기(2.5cm) · 픽셀 스프라이트 VFX 세트 · UI 재스킨 = 각각 후속 행.
- 한글 픽셀 폰트 라이선스·글리프 커버리지 확인 뒤 D8 확정.
