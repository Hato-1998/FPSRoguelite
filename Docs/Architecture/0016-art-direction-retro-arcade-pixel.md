# 0016. 아트 방향 = 레트로 아케이드 픽셀 — 복셀 형태 + 픽셀 스프라이트 + CRT, 어두운 무대 위 팝 액센트

- 상태: 채택 (사용자 결정 2026-09-03 방향 전환 · 2026-09-06 세부 3건 확정 · **육안 검증 = 컨셉 시트 승인 대기**, 인게임 검증은 후속 행)
- 날짜: 2026-09-06
- 되돌리기 비용: 비쌈 (복셀 메시·요소ID LUT 머티리얼·픽셀 스프라이트·UI 스킨이 전부 이 룩에 종속된다. 색 규칙(`ArtDirection.md §A`)만은 룩과 독립이라 살아남는다)
- 대체 대상: **Synty 셀/툰 전체 통일 룩**(2026-07-10 확정 · 2026-07-18 U21 락 · 2026-09-03 폐기, `Concept.md §1-C-9` "아트 방향" 항목) 과 **Tron 트랙**(`Docs/U22a-B_TronVisual_Prompt.md`, 2026-07-23) 의 "게임 전체 전환" 조건부 P2. Tron 문서 **§1 제1원리 5건 · §3 밋밋 방지 5레버는 계승**한다.
- 계승 대상: [ADR 0007](0007-enemy-swarm-render-path-cpd.md) (적 = 공유 base material + CPD 개체차) · [ADR 0012](0012-authored-arena-sublevel-and-collision-bake.md) 불변식 2 ("렌더 메시는 껍데기") · [ADR 0013](0013-enemy-tier-axis-and-elite-gas.md) (티어 축) · [ADR 0015](0015-first-person-gun-only-hidden-arms-driver.md) (1P = 총만) · `Docs/SSOT/ArtDirection.md §A` (색 대역 — **수치·hue 무변**)
- 시각 부록: 컨셉 시트 아티팩트 「FPSRoguelite Arcade Pixel Concept Sheet」(아트보드 6장 — 키 비주얼 1P 프레임 / 팔레트 재매핑 / 형태 언어 / 모티프·로그라이트 매핑 / HUD 픽셀 모크 / 적 라인업·3구역). 링크 = 보드 행 로그. 이 문서가 결정, 시트는 그 그림이다.

## 맥락

2026-09-03 사용자가 월드 모티프를 **레트로 아케이드 사이버스페이스**로 돌렸고, 같은 날 아케이드 룩 프로토(아레나 코너 네온 격자 + CRT 포스트프로세스, 보드 「아케이드 룩 프로토 + 육안 게이트」 완료 `037e681f`)와 2026-09-05 복셀 유령 적 「쩝쩝이」 파일럿이 화면으로 방향을 확인했다. 그 프로토의 한계도 같이 보였다 — 지금 아레나는 **검은 면 + 발광 격자선**뿐이라 "Tron" 으로는 읽혀도 **"픽셀 게임"으로는 읽히지 않는다**. 쩝쩝이(복셀)와 아레나(선)가 다른 언어를 쓰고 있었다.

2026-09-06 사용자가 레퍼런스를 제시했다: **롤(LoL) 아케이드 스킨 시리즈**(컨셉·픽셀 게임 요소·색감만 — 캐릭터·의상·포즈는 참고하지 않는다)와 **웹툰 『전자오락수호대』**(게임 세계를 지키는 수호자 모티프). 메인 키워드 = **FPS · 아케이드 · 픽셀 · 로그라이트**. 요청 = *"컨셉아트를 작성해서 메인 비주얼적인 부분을 픽스하고 가자."*

착수 전 지적 4건과 그에 대한 사용자 결정(2026-09-06):

| # | 지적 | 결정 |
|---|---|---|
| 1 | 레퍼런스의 주황·마젠타 배경은 **적 예약 대역**(`§A-3-4`)과 정면 충돌. 4인 협동 × 적 200~300 에서 배경이 뜨거우면 스웜 스캔이 죽는다 | `§A-0` 공식 그대로 — **hue 만 가져오고 명도·채도를 대역에 재매핑**, 뜨거운 색은 적·경계 밖·UI 보상에만 |
| 2 | "픽셀"을 3D 1인칭에 넣는 방식이 둘 — 화면 전체 픽셀화 PP vs 복셀 형태 + 스프라이트 + CRT | **복셀 + 스프라이트 + CRT.** 전체 픽셀화 PP 는 조준 정밀도와 원거리 적 판독을 깨서 기각 |
| 3 | 현행 프로토는 "선"(Tron), 아케이드 픽셀은 "면·블록" — 배합 규칙이 없다 | 아래 D3 |
| 4 | 롤 아케이드는 밝고 팝, 현행은 어두운 무대 | **어두운 무대 + 팝 액센트.** `§A-1` 환경 대역 유지 |

보드 행 본문(2026-09-03 정정)이 ADR 에 요구한 판단 2건 — **무기 악센트 색과 `§A-3-5` 봉인 조항의 충돌**, **복셀 룩이 CPD 병합 자격을 깨지 않는가** — 는 D5·I3 에서 답한다.

## 결정

**D1. 룩 = 레트로 아케이드 픽셀.** 픽셀은 세 층에 산다 — ① **형태 = 복셀**(3D 픽셀; 적·프롭·환경 블록·1P 무기) ② **평면 요소 = 픽셀 스프라이트**(VFX·픽업·UI 아이콘, 포인트 필터, 팔레트 제한) ③ **화면 = CRT 포스트프로세스**(스캔라인·색수차·비네트, 현행 `PP_Arcade` 값 유지: 스캔라인 270 · 강도 0.15 · 곡률 0). **화면 전체 저해상도 픽셀화는 쓰지 않는다**(I6).

**D2. 환경 = 어두운 무대, 팝은 액센트.** `§A-1` 대역표 무변(환경 V 6~40 · S 15~40). 기판 substrate 5색(`§A-3-1`)이 화면 70% 이상. 팝한 색(금·주황·연두·마젠타)은 합쳐 **화면 10% 미만**이고 자리가 정해져 있다 — 통행 정보(배선), 파괴물 코어, 픽업, VFX, UI, 경계벽 밖.

**D3. 면은 안, 선은 밖.** 플레이 공간(콜리전이 있는 모든 것) = **복셀 면** — 평면 채색 + 같은 대역 안의 어두운 격자선(복셀 경계). 밝은 선은 오직 **L2 배선**(통행 가능 = 밝다, 0010 D8)만 든다. 경계벽 바깥(플레이 불가) = **선** — 회로기판 트레이스·와이어프레임 부유 기하(Tron 유산). 마젠타는 여기서만 강하게(`§A-3-6`). Tron §3 의 5레버(구역 색·밝기 대비·밀도 변주·반사·실루엣)는 아케이드로 번역해 유지한다 — "반사"만 젖은 바닥 대신 **배선 발광**으로 대체.

**D4. 복셀 격자 계층(제안 초기값, PIE 육안 조정 대상).**

| 클래스 | 격자 | 근거 |
|---|---|---|
| 환경(블로커·벽·프롭) | **5 cm** | 콜리전 셀 100cm · 문턱 45cm · 60cm 가 **전부 정수배**(20·9·12). 면은 평평, 격자는 머티리얼 선 — 지오메트리를 5cm 로 쪼개는 게 아니다 |
| 적·픽업 | **7.5 cm** | 쩝쩝이 기존값(165cm = 22층). 먼 거리에서도 덩어리로 읽히는 굵기 |
| 1P 무기 | **2.5 cm** | 카메라 30~60cm 앞이라 적 격자의 1/3 — 화면상 픽셀 크기가 적과 비슷해진다 |

한 오브젝트 안에서 격자는 하나(I4). 클래스 사이의 격자 차이는 **거리가 화면 픽셀 크기를 맞춰 주므로** 허용한다.

**D5. 색 = `ArtDirection.md §A` 그대로. 레퍼런스 hue 의 재매핑 표는 `§B-2`.** 무기 악센트는 사용자 결정(2026-09-03)대로 **파란 계열**이며, `§A-3-5` 봉인 조항("환경·프롭·UI 장식 어디에도 고채도 아군색 금지")과의 충돌은 **예외 조항으로 명문화**한다 — **무기는 프롭이 아니라 캐릭터의 일부**다. 1P 무기 이미시브 = `#8B6BFF`(자기 자신), 3P 팀원 무기 = `#4FD8FF` 계열을 **채도 낮춰**(아군 아웃라인과 싸우지 않게). 무기가 월드에 놓이는 순간(드롭·상점 프롭이 생기면) 그건 프롭이라 UI 보상색(`#FFC24A`) 규칙을 따른다. 별도 블루를 발명하지 않는 이유 = 규칙 하나("파랑 = 우리 편")를 지키는 게 학습 비용이 싸다.

**D6. 픽션 어휘 = 게임 세계 다이브 + 수호자.** `Concept.md §1-C-9`(다이브) 위에 『전자오락수호대』 모티프를 얹는다: 플레이어 4인 = 접속한 히어로(차가운 색), 적 = 세계를 갉아먹는 **버그·글리치**(뜨거운 색), 억제기 = 스테이지를 지배하는 **캐비닛 코어**. 로그라이트 어휘의 아케이드 번역: 런 = 1코인 플레이 / 레벨업 카드 = 파워업 / 스테이지 전환 = **STAGE CLEAR**(`§A-2` Ultimate 자리) / DBNO = **CONTINUE? 카운트다운**(동료가 오면 멈춤) / 런 종료 = GAME OVER + 하이스코어. 문구는 콘텐츠(LOC0 String Table)지 코드가 아니다.

**D7. 제작 경로 = 셋, UE VoxWrap 은 배제.** ① **절차 생성**(파이썬 → OBJ, 쩝쩝이 방식) — 일반 적·프롭·반복물 ② **손 저작**(Blockbench 등 외부 DCC — 사용자 작업) — 보스·1P 무기·랜드마크 ③ **기존 메시 변환**(Blender Remesh > Blocks) — 후보, 결과가 크리스프 큐브인지 건별 확인. UE5 `ApplyMeshSolidify`(VoxWrap)는 결과가 둥글어 배제. **텍스처 없음** — 색 = 요소ID LUT(`floor(UV.u)` 계약, 쩝쩝이 12요소) 또는 정점색 + 격자선 + 이미시브. 사진·노이즈 텍스처 금지(`§A-5`).

**D8. 폰트 = 픽셀 폰트 2종 후보(후속 행에서 확정).** Latin·숫자 = Press Start 2P 계열(OFL) / 한글 = Galmuri 계열(OFL). 컨셉 시트는 Press Start 2P + Noto Sans KR 로 그렸다(한글 픽셀 폰트는 Google Fonts 에 없다). 확정은 LOC0 String Table 정합과 함께 별도 행.

## 근거 (핵심원칙 4 — 3줄)

1. **제1원리** — 이 게임의 화면은 *적 수백 마리를 읽는 1인칭*이다. 복셀은 **실루엣이 덩어리로 읽히고**(디테일이 격자로 제한되므로 노이즈가 생기지 않는다), 어두운 무대는 **VFX·적 코어·픽업의 헤드룸**을 남긴다. Tron §1 의 다섯 근거(디테일 텍스처 불필요 · 싸다 · 스웜 가독성 · 머티리얼 통일 · 블록 지오메트리 정합)는 아케이드 픽셀에서 전부 살아남고, 여기에 "픽셀 = 격자 하나로 저작 규칙이 정해진다"가 더해진다.
2. **엔진 기본값·기존 인프라와의 관계** — 렌더 경로는 **바꾸지 않는다.** 적 = 0007 의 공유 base material + CPD(`_Proc` 가 CPD 에 상태ID·진입시각·rate 를 쓰고, 읽는 쪽 = 복셀 머티리얼 WPO 가 그 빈 자리에 들어간다). 아레나 = 0012 의 "렌더 메시는 껍데기" 위에서 렌더 메시·머티리얼만 교체. 크로스헤어(U12 절차 SDF)·HUD 위젯 3종(세터·값 소스 유지, 스킨만)·`PP_Arcade`·쩝쩝이 파이프라인은 그대로 쓴다. 엔진의 스크린 정렬 픽셀화(머티리얼 PP 다운샘플)는 *오브젝트가 회전하면 깨지고 1P 무기가 최악*이라 쓰지 않는다 — 픽셀은 오브젝트에 붙는다.
3. **프로젝트 제약과의 정합** — 4인 협동 기준선: 아군 = 차가운 색 실루엣, 적 = 뜨거운 색 + 코어 1개 — 이 규칙은 룩과 독립이라 룩이 바뀌어도 협동 가독성이 무너지지 않는다. 서버권위·복제 무접촉(전부 렌더·콘텐츠). 콜리전 100cm 셀과 45/60cm 문턱이 환경 격자 5cm 의 정수배라 저작 규칙 하나로 두 세계가 맞는다.

## 불변식

| # | 문장 | 없으면 깨지는 것 | 등급 |
|---|---|---|---|
| I1 | 적 = 뜨거운 쪽 / 아군 = 차가운 쪽(`§A-3-4`·`§A-3-5`)은 **룩과 독립인 절대 규칙**이다. 이 ADR 은 그 위에 얹힌다 | 4인 협동 × 스웜에서 배경·적·아군이 섞인다 — 복구 불가 | 구조적 |
| I2 | 룩 교체는 **렌더 메시·머티리얼·스프라이트·UI 스킨**만 건드린다. 콜리전·마스크·플로우필드 베이크·서브레벨 위상 무영향(0012 불변식 2) | 에디터 배치와 인게임 판정이 갈린다 | 구조적 |
| I3 | 적 복셀 메시는 **공유 base material + CPD 개체차**다. per-actor MID·개체별 머티리얼 금지(0007). 개체차(색·상태·입 개폐)는 CPD 슬롯과 요소ID LUT 로만 | 300마리 드로우콜 +84(0007 실측) 재발 | 구조적 |
| I4 | **한 오브젝트 한 격자.** 한 메시 안에 두 격자 크기를 섞지 않는다(믹셀 금지). 클래스 간 격자 차이(5/7.5/2.5)만 허용 | "픽셀 게임"으로 안 읽히고 "저폴리 메시"로 읽힌다 | 저작 |
| I5 | 환경 면은 `§A-3-1` 5색 안에서만 칠한다. 밝은 것은 **배선·파괴물 코어·픽업·VFX·UI·경계 밖**뿐 | 환경이 대역을 먹어 그 위에 얹을 게 없어진다(`§A-0` ①) | 저작 |
| I6 | **화면 전체 픽셀화 PP 금지.** 픽셀은 오브젝트(복셀·스프라이트)와 스캔라인에 있다 | 조준 정밀도·원거리 적 판독·회전 시 픽셀 흔들림 | 구조적 |
| I7 | **텍스처 없음.** 색 = 요소ID LUT / 정점색 + 격자선 + 이미시브. 사진·노이즈 텍스처 금지 | 복셀 위에 노이즈가 얹혀 격자가 죽는다(`§A-5`) | 저작 |
| I8 | 무기 악센트는 `§A-3-5` 예외다 — **무기는 캐릭터의 일부**일 때만. 월드에 놓인 무기는 프롭 규칙 | 아군색이 프롭에 새어 "우리 편" 규칙이 흐려진다 | 저작 |

## 검토한 대안

### 안 A — 화면 전체 저해상도 픽셀화 포스트프로세스 — **기각(사용자 결정 2026-09-06)**
가장 "픽셀 게임"처럼 보이는 지름길. 그러나 프레임 전체를 다운샘플하면 **크로스헤어 정밀도와 원거리 적(수 픽셀짜리) 판독**이 같이 깎이고, 픽셀이 화면에 정렬돼 오브젝트가 회전하면 표면 픽셀이 흐른다(1P 무기가 최악). 적 200~300 을 읽어야 하는 이 게임에서 정보 채널을 깎는 룩은 제1원리 위반. 근거리 전용·원거리 전용으로 나누는 혼합안도 경계 지글거림 리스크와 셰이더 비용이 추가돼 보류.

### 안 B — 중간 밝기 평면 채색(픽셀 게임처럼 환경도 밝게, 레퍼런스에 가깝게) — **기각(사용자 결정 2026-09-06)**
롤 아케이드의 팝한 인상에 가장 가깝다. 대가 = `§A-1` 대역표 개정 — 환경 V 를 50 근처로 올리면 캐릭터(V 25~70)·VFX 대역과 겹쳐 **적·VFX 헤드룸이 줄고** 스웜 가독성이 PIE 실측 없이는 보증되지 않는다. "어두운 무대 + 팝 액센트"가 같은 인상을 액센트 면적으로 내면서 대역표를 건드리지 않는다. 실측 후 환경 대역 상단을 5~10 올리는 정도는 후속 조정 범위.

### 안 C — 셀/툰(SRS) 전체 통일 룩 유지 — **폐기(2026-09-03 사용자 결정, 이 문서가 기록)**
2026-07-10 확정·07-18 U21 락. 폐기 사유 = 도시맵 트랙 자체가 ADR 0010 에서 기각되고 Synty 도시·무기 팩 콘텐츠가 삭제됐으며(`31fee185`), 아레나 위상에서 셀 아웃라인이 줄 이득(메시 디테일 정리)이 복셀에서는 격자 자체가 대신한다. SRS 스택(`srs-postprocess-stack-fog-haze`)·Custom Depth 실측(1.33ms)은 기록으로 유효.

### 안 D — 순수 Tron 선 룩(현행 프로토 그대로) — **부분 계승**
프로토 판정(2026-09-03)에서 "룩 방향은 맞다"가 나왔지만, 검은 면 + 발광 선만으로는 픽셀 게임으로 읽히지 않고 복셀 적과 언어가 갈린다. 선 언어는 **경계 밖**으로 보내 살리고(D3), 플레이 공간은 면으로 바꾼다. Tron §1·§3 는 계승.

### 안 E — 스크린 정렬 픽셀 셰이더(오브젝트별 머티리얼에서 픽셀 스냅) — **기각**
복셀 메시 없이 머티리얼만으로 격자를 흉내내는 안. 실루엣을 못 바꾸므로 윤곽은 매끈한데 표면만 계단이 되어 **가짜 픽셀**로 읽힌다. 새 메시가 필요하다는 보드 조사 결론(2026-09-03)과 같다.

## 검증 시나리오 (사용자 판정 — 결과를 여기 기입)

- [ ] **컨셉 시트 승인** — 키 비주얼 아트보드가 "FPS · 아케이드 · 픽셀 · 로그라이트" 네 키워드로 읽히는가. 통과 = 이 ADR 채택 확정.
- [ ] **쩝쩝이 MI 색 확정**(ART-후속 행, 검증중) — `§A-3-4` 안에서 몸통 색을 정하고 컨셉 시트의 `#9E4560` 가안과 대조.
- [ ] **아레나 블록 복셀 면 프로토**(후속 행) — `L_Map_1` 블로커 1종에 5cm 격자 머티리얼 + `§A-3-1` 채색. 육안: 검은 면이 아니라 "복셀 면"으로 읽히는가.
- [ ] **4인 PIE 가독성** — 적 200 + 아군 3 실루엣: 배경(자두 회로)이 적으로 오독되지 않는가, 아군 아웃라인이 배선과 안 섞이는가.
- [ ] **성능** — 적 300 복셀 메시에서 다이나믹 인스턴싱 병합이 유지되는가(`stat RHI` 드로우콜, 0007 A/B 방식). 쩝쩝이 4,606 tris × 300 = 1.4M tris — 예산 초과면 **면 병합(greedy meshing)** 을 생성기에 넣는다(격자 변경 아님).

## 논의 중 제기된 반론

- **"격자 3종이면 그게 믹셀 아닌가"** — 픽셀 아트의 믹셀 금지는 *한 화면 안 한 평면*의 규칙이다. 3D 에서는 거리가 화면 픽셀 크기를 바꾸므로 클래스마다 격자를 달리 해야 오히려 화면에서 균일해진다(1P 무기 2.5cm 가 30cm 앞에서, 적 7.5cm 가 10m 밖에서 비슷한 화면 픽셀). 금지는 **한 오브젝트 안**에만 건다(I4).
- **"어두운 무대는 레퍼런스와 다르다"** — 맞다. 레퍼런스는 히어로 아트라 채도·명도를 다 쓴다(`§A-0` ①). 같은 인상을 내는 수단은 **밝은 것의 면적**이 아니라 **밝은 것의 자리**다 — 픽업·코어·UI·경계 밖이 팝하면 화면은 팝하게 읽힌다. 안 B 를 열어 두었으니 PIE 뒤 필요하면 대역 상단을 조정한다.
- **"복셀 메시는 폴리가 많다"** — 사실이다(쩝쩝이 4.6k tris). 다만 이 비용은 격자 크기가 아니라 **면 병합 여부**가 정한다. 절차 생성기에 greedy meshing 을 넣으면 같은 격자에서 tris 가 수 배 줄어든다 — 검증 시나리오 마지막 항목.
- **"무기에 아군색을 쓰면 봉인 조항 위반"** — 문언상 그렇다. 그래서 I8 로 **예외를 명문화**했고, 조건(캐릭터의 일부일 때만)을 붙였다. 별도 블루를 만들면 "파랑 = 우리 편" 규칙이 둘이 된다.
- **"컨셉 시트가 그림인데 왜 ADR 인가"** — 시트는 사라지거나 편집될 수 있다. 결정(D1~D8)과 불변식(I1~I8)은 여기 있어야 다음 에셋 작업이 해석 없이 따를 수 있다.

## 영향받는 기존 결정

- [0007](0007-enemy-swarm-render-path-cpd.md) — 계승. `_Proc` 의 CPD 쓰기를 읽는 쪽 = 복셀 머티리얼(후속 행). 병합 자격 불변(I3).
- [0012](0012-authored-arena-sublevel-and-collision-bake.md) — 계승. 불변식 2·6·7 그대로. 환경 격자 5cm 는 **렌더 전용**이고 45~60cm 저작 금지 대역은 그대로.
- [0013](0013-enemy-tier-axis-and-elite-gas.md) — 계승. 엘리트 = 같은 실루엣 × 1.3 + 림 `#FF1E7A` + 코어 2개로 티어를 **형태**로 읽게 한다.
- [0015](0015-first-person-gun-only-hidden-arms-driver.md) — 계승. 1P 비주얼 = 총 하나이므로 **복셀 무기(2.5cm)** 가 1P 룩의 전부. **하드서피스 라이플 트랙**(`Docs/RifleHardSurface_ResumePrompt.md`, 일시정지)은 복셀 무기 저작으로 방향 전환 — 재개 시 신규 행.
- **Tron 트랙** `Docs/U22a-B_TronVisual_Prompt.md` — §1(제1원리 5건)·§3(5레버) 계승, P2("게임 전체 전환, 조건부")를 이 문서가 닫는다. 그 문서는 삭제하지 않는다.
- **Synty 셀/툰 락**(`Concept.md §1-C-9` 아트 방향 항목, 메모리 `synty-anime-cel-art-pivot`) — 폐기. 무기 Synty Military 백본·SRS 렌더러 확정은 무효. 3P 캐릭터는 NEON-V 트랙(후행 행)에서 이 룩으로 재검토.
- **`Docs/SSOT/ArtDirection.md`** — §A 무변. **§B(모티프·형태·픽셀 규칙) 신설** = 이 ADR 의 저작 규칙 본문.
- **HUD 위젯 3종**(`a97068c7`)·U12 크로스헤어·`PP_Arcade` — 배선·값 소스·SDF 유지, 스킨·픽셀 스냅만 후속.
- **쩝쩝이 파이프라인**(`Scripts/gen_voxel_chomper.py` 외) — 이 ADR 의 D7 ① 경로의 기준 구현. 요소ID LUT 계약이 표준이 된다.
- **ART-후속 행**(복셀 적 파일럿, 검증중) — 이 ADR 이 선행이었으나 사용자 결정으로 병행됐다. 색 확정 시 `§B-2` 재매핑 표와 대조.
