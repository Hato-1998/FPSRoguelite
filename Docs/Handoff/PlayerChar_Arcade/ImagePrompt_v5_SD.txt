====================================================================
ENE — 플레이어 3P 캐릭터 · 삼면도 생성 프롬프트 v5 (Stable Diffusion 경로)
====================================================================
v1~v4 = 제미나이(자연어) 경로. v5 = **Stable Diffusion(danbooru 태그) 경로**.
둘은 경쟁 관계가 아니라 **같은 스펙의 두 표현**이다 — 스펙 정본은 어느 쪽도 아니고
`Docs/PlayerCharacter_ResumePrompt.md` §2 다. 나은 결과가 나오는 쪽을 고른다.

SD 가 제미나이보다 유리한 지점 = **네거티브 프롬프트**. 제미나이에 "주황 넣지 마"라고 쓰는 건
부탁이지만, SD 네거티브의 `orange, red, magenta`는 실제 억제 메커니즘이다.
레퍼런스 [4] 헤드기어가 원본이 주황이라 이 지점이 가장 새기 쉬운데, 그걸 구조적으로 막는다.

🔧 **툴 설치·기동은 이 문서 범위 밖이다**(리포 밖으로 분리, 2026-09-08).
   체크포인트·ControlNet 후보, 설치 실측, 시작기 = F:\StableDiffusion\ 로 옮겼다
   (SD_Forge_Setup.md · run_sd_forge.bat · stop_sd_forge.bat).
   이 문서는 **무엇을 그릴지**만 다룬다.

--------------------------------------------------------------------

## 2. 🔑 전략 — 한 장에 3뷰를 욕심내지 않는다

제미나이 경로는 "한 장에 3뷰"였다. 일관성을 AI 의 선의에 맡기는 방식이라 v1 에서 실제로 깨졌다.
**SD 에서는 그럴 필요가 없다** — Meshy 는 어차피 낱장으로 받는다.

1. **정면을 먼저 뽑아 확정**한다(시드 고정).
2. 그 이미지를 **`reference_only` ControlNet** 에 물리고 프롬프트만 `from side` / `from behind` 로 바꿔 측면·후면을 뽑는다.
3. 세 장의 **키·배율을 후처리로 맞춘다**(같은 캔버스 높이로 리사이즈).

이게 "세 뷰 일치"를 **희망이 아니라 절차**로 만든다 — 이 트랙에서 반복적으로 깨진 항목이 바로 그것이다.

## 3. 프롬프트 (Illustrious / NoobAI 계열 기준 · danbooru 태그)

### 3-1. Positive - 정면  (v6 의상개정 2026-09-08)

🔴 **토큰 순서가 곧 가중치다.** CLIP 은 75토큰 단위로 잘려 뒤쪽 청크일수록 영향이 약하다.
스타일·비례를 **1번째 청크**에 두고 의상·소품을 뒤로 보낸다. 대문자 강조는 CLIP 에서 무의미하며
강조는 `(term:1.3)` 문법으로만 준다. **1.4 이상은 쓰지 않는다**(SDXL 에서 형태가 탄다).

**v6 변경** = 의상을 **미니멀 → 레이어드 사이버펑크 스트리트**로. 키워드 = 사이버펑크 × 스트리트.
v5 초판(Illustrious) 결과가 "우주복·의료복"으로 읽혔다 — 흰 크롭재킷 + 검정 보디수트만으로는
스트리트가 안 된다. 레이어·하네스·파우치·볼륨 칼라가 그 인상을 만든다.
등신·머리폭·사지 테이퍼는 v5 에서 미달이라 **가중치를 올렸다**.

```
masterpiece, best quality, very aesthetic, official art,

(thick outline:1.3), (flat color:1.35), (cel shading:1.2), hard shadow, bold lineart,
(limited palette:1.4), (three color scheme:1.3), off-white and black and light blue only,
(front view, facing viewer:1.3), (full body:1.2),
(sky blue glowing trim:1.35), (light blue accents:1.25), (pale blue hair:1.25), (light blue tinted goggles:1.3),
(off-white jacket:1.4), (black leggings:1.3),

(head wider than shoulders:1.35), (large head:1.35), (short legs:1.3), (petite:1.2), (chibi:0.5),
short neck, narrow shoulders, chibi-like proportions, rounded body,
(thick rounded limbs:1.25), (puffy sleeves:1.2), (thick legs:1.25), (narrow hips:1.4), (narrow pelvis:1.3), (fitted shorts:1.2), (slim waist:1.15), toy-like, (oversized shoes:1.25),

1girl, solo, full body, standing, a-pose, arms slightly away from body, open relaxed hands, empty hands,
simple background, grey background,

blunt bangs, chin-length bob cut, single pale streak in bangs,
over-ear headphones, cable,

(oversized jacket:1.2), (fully closed jacket:1.45), (fully zipped:1.4), (long jacket:1.4), (jacket covers entire torso:1.35), (hem below mid thigh:1.3), (high standing collar:1.25),
off-white jacket, panel lines, (symmetrical:1.25), (simple design:1.25), (no accessories:1.3), bare arms,
(no visible shirt:1.25), (no visible bottoms:1.3), black leggings, covered legs,
oversized platform sneakers, chunky sneakers,

(glowing trim on clothes:1.2), back panel
```

**의상 설계 의도(그림에서 확인할 것)**
- **레이어가 스트리트를 만든다** — 보디수트(베이스) → 크롭 재킷(오버사이즈) → 하네스·벨트(위에 얹힘).
  세 층이 겹쳐 보여야 한다. 한 겹이면 v5 처럼 우주복으로 읽힌다.
- **덩어리 소품 소수** — 파우치 2~3개를 **크게**. 얇은 끈·체인·피시넷은 넣지 않는다
  (복셀 3.75cm 에서 1칸 미만은 소멸한다 - §복셀 제약).
- **퍼 칼라 대신 각진 볼륨 칼라** — 털은 복셀에서 표현 불가.
- **비대칭은 어깨패드 하나만** — 좌우 비대칭을 늘리면 복셀 생성기의 좌우대칭 단언을 못 쓴다.

🔴 **색 제약(레퍼런스에서 가장 크게 번역되는 부분)**
레퍼런스는 보라·블루 그라디언트가 주도한다. 그러나 보라 `#8B6BFF` 는 §A-3-5 에서
**"자기 자신"** 예약색이다 — 대면적으로 쓰면 4인 협동에서 팀원 구분 신호와 충돌한다.
→ **시안 `#4FD8FF` 가 주도하고 보라는 소면적 액센트**(`small purple accent` 한 토큰만 준 이유).
그라디언트 자체도 §A-5·I7(평면 채색) 위반이라 쓰지 않는다.

### 3-2. Negative (여기가 SD 의 진짜 강점이다 — 문장이 아니라 억제 메커니즘)
```
orange, red, magenta, pink, amber, yellow, warm colors, orange tint, red glow,
(purple:1.3), violet, lavender, purple hair, purple tint, gradient,
(denim:1.35), jeans, blue jeans, blue pants, brown, beige, khaki, olive, multicolored, colorful,
trench coat, robe, dress,
(asymmetrical:1.3), asymmetrical legwear, uneven hemline, single pant leg, mismatched legs,
(wide hips:1.4), (thick hips:1.3), (flared shorts:1.3), puffy shorts, bulky shorts, hip pouches, wide stance,
(open jacket:1.4), (unzipped:1.4), (cropped jacket:1.4), midriff, exposed navel, (tank top:1.3), visible shirt,
exposed chest, cleavage, (short jacket:1.3), (flared hem:1.3), peplum, jacket flap, (visible shorts:1.25),
floating clothes, detached clothing, protruding panel, side flap,
(harness:1.35), (strap:1.35), (belt:1.3), (pouch:1.35), buckle, d-ring, bag, holster, sling,
(gloves:1.35), fingerless gloves, arm guard, shoulder pad, armor, bracer, wristband,
(floating ring:1.4), (energy swirl:1.4), glowing ring, aura, halo, swirl, ribbon, sash, cape,
(accessories:1.35), jewelry, necklace, choker, badge, emblem, patch, chain, buttons, zipper pull,
white thigh strap, white thigh pouch, long shorts,
(green:1.35), lime, neon green, spring green, teal, turquoise, green glow,
(lineart:1.2), sketch, monochrome, uncolored, white clothes only, sticker, white border, outline border,
pure white, pure black, white background,

photorealistic, realistic, 3d render, photo, gradient, soft shading, airbrush, blurry, noise, texture, film grain,
(thin limbs:1.3), (skinny legs:1.35), (thin thighs:1.3), stick legs, skinny, slender, (tall:1.2), (long legs:1.35), realistic anatomy, fashion illustration, elegant proportions, small head,

weapon, gun, sword, grenade, holding, ribbon, scarf, tentacle, squid, ink, hat, cap, backpack,
bare legs, bare thighs, thighs, exposed skin on legs,

text, watermark, signature, logo, dimension lines, arrows, border,
multiple girls, 2girls, extra limbs, bad hands, bad anatomy, worst quality, low quality, jpeg artifacts,

fur trim, fur collar, fishnet, chain, thin straps, lace, frills, gradient clothes, holographic,
(bare thighs:1.3), (bare legs:1.3), naked legs, skirt, dress, one-piece bodysuit, (leggings only:1.3), no shorts, (black shorts:1.3), dark shorts
```
> 🔴 네거티브 첫 줄이 **`§A-3-4` 적 예약 대역 차단**이다. `#FF3B4E`·`#FF6B2C`·`#FF1E7A` 가 화면에
> 들어오면 4인 협동 × 적 200~300 에서 아군이 적으로 읽힌다(ADR 0016 **I1**, 복구 불가 등급).
> 특히 레퍼런스 [4] 헤드기어가 **원본이 주황**이라 여기가 가장 새기 쉽다.

### 3-3. 측면 / 후면 (정면 확정 후, `reference_only` 물린 상태에서)
`front view, facing viewer` 만 아래로 교체하고 **나머지는 한 토큰도 바꾸지 않는다**:
- 측면 → `from side, profile, facing right`
- 후면 → `from behind, back view, facing away from viewer`

가중치 교체와 뷰별 추가 네거티브는 §3-3-2·§3-3-3, `reference_only` 유닛은 §3-3-4.
실행은 `control/gen_sd_cn.py`(이 문서를 읽으므로 문서와 갈라지지 않는다):
```
python gen_sd_cn.py <depth.png> <tag> <depth가중치> <시드> <폭> <뷰> [레퍼런스.png|-] [ref가중치]
python gen_sd_cn.py depth_ene_side.png v20_side 0.45 1234567 832 side ../sd_out/v19_fullclosed_w045_seed1234567_112834.png 0.45
```

### 3-3-1. 🔴 레퍼런스는 "역할별로 크롭"해서 넣는다 (2026-09-08 추가)

**IP-Adapter 는 속성을 분해하지 못한다.** 제미나이에는 "1번에서 헤어만, 2번에서 의상만"이라고
말로 지시할 수 있지만(LLM 프론트엔드가 그걸 읽는다), **단일 패스 IP-Adapter 는 레퍼런스
이미지 전체의 특징을 뭉쳐서 가져온다.** 말로 나눌 수 없다.

→ **이미지 자체를 잘라서 물리적으로 제한한다.**

| 유닛 | 넣을 이미지 | 크롭 범위 | 가중치(초기값) |
|---|---|---|---|
| CN #0 | `Ref1_Leiya_*` | **머리만**(정수리~턱, 헤어 실루엣 포함) | 0.5~0.6 |
| CN #1 | `Ref2_Costume_*` | **몸통~다리**(얼굴 제외) | 0.5~0.6 |
| CN #2 | `Ref4_Headgear_*` | **고글·헤드폰만**(머리색 안 들어오게 얼굴 위쪽만) | 0.35~0.45 |

- Forge 의 Integrated ControlNet 은 **유닛을 여러 개 쌓을 수 있다.** 유닛마다 이미지·가중치가 따로다.
- ⚠️ 유닛을 겹칠수록 프롬프트의 지배력이 떨어진다. **총 가중치 합이 1.5 를 넘지 않게** 시작하고,
  형태가 무너지면 먼저 가중치를 내린다.
- [3] 체형은 IP-Adapter 로 넣지 **않는다** — 비례는 §3-1 의 태그와 §1-5 OpenPose 로 잡는 게 정확하다.
  IP-Adapter 로 넣으면 그 그림의 의상·촉수까지 딸려온다.
- 크롭본은 원본을 덮지 말고 `_crop` 접미어로 따로 저장한다(원본은 판정 대조용).

### 3-3-2. 🔴 뷰별 추가 네거티브 (2026-09-08 — 3/4 뷰 재발 차단)

v8b 측면이 정측면이 아니라 **3/4 뷰**로 나왔다. 원인은 둘이었다.
1. depth 블록아웃의 A-포즈 팔이 프로필인데도 화면을 **가로질러 45°** 로 뻗어 있어 "앞으로 내민
   팔"로 읽혔다 → `control/make_depth.py` 의 side 분기를 프로필 전용으로 재작성해 해결
   (팔은 거의 수직 + 앞뒤 비대칭 = 어느 쪽이 앞인지의 단서).
2. 프롬프트에 정면을 **억제**하는 항목이 아예 없었다. 뷰 절을 바꾸는 것만으로는 모델의
   "정면 선호" prior 를 못 이긴다 — 정면을 네거티브로 눌러야 한다.

아래 블록이 `### 3-2.` 네거티브 **뒤에 이어붙는다**(`gen_sd_cn.py` 가 뷰에 따라 자동으로 붙인다.
정면 생성 시에는 아무것도 붙지 않는다).

**측면(`side`)**
```
(front view:1.5), (facing viewer:1.5), (three-quarter view:1.45), 3/4 view,
turned toward viewer, frontal pose, facing camera, (looking at viewer:1.3), (spread arms:1.2)
```

**후면(`back`)**
```
(front view:1.5), (facing viewer:1.5), (three-quarter view:1.45), 3/4 view,
turned toward viewer, frontal pose, facing camera, (looking at viewer:1.4),
(face:1.25), facial features, eyes, mouth, nose
```

⚠️ 후면의 `(face:1.25)` 는 **얼굴만** 억제한다 — 고글 스트랩·헤드폰 밴드는 뒤에서도 보여야 하므로
`goggles`·`headphones` 는 네거티브에 넣지 않는다. 결과에서 헤드기어가 사라지면 이 값을 먼저 내린다.

### 3-3-3. 뷰 절 가중치 = 정면 1.3 / 측면·후면 **1.5**

`### 3-1.` 의 `(front view, facing viewer:1.3)` 을 통째로 `(<뷰 절>:1.5)` 로 교체한다.
측면·후면은 모델의 prior 를 거스르는 방향이라 정면과 같은 가중치로는 못 이긴다.
**1.5 를 넘기지 않는다** — §3-1 의 경고대로 SDXL 에서 1.4~1.5 위는 형태가 타기 시작한다.

### 3-3-4. `reference_only` 유닛 — §2 절차 2단계의 실제 구현

§2 가 "정면을 확정한 뒤 그 이미지를 `reference_only` 에 물려 측면·후면을 뽑는다"고 적어 놓고
**구현이 없었다**(v8b 는 depth 만 물고 뽑혀서 의상이 통째로 다른 캐릭터가 나왔다).
`gen_sd_cn.py` 에 두 번째 ControlNet 유닛으로 추가했다 — 선택 인자라 켜고 끌 수 있다.

| 항목 | 값 | 근거 |
|---|---|---|
| module | `reference_only` | Forge Integrated ControlNet 내장. model 은 `None` |
| image | **정면 확정본**(현재 `sd_out/v19_fullclosed_*.png`) | 세 뷰를 같은 캐릭터로 묶는 유일한 장치 |
| weight | **0.45** 시작 | 높이면 레퍼런스의 **구도(정면)** 까지 따라와 3/4 가 재발한다 |
| guidance_end | **0.55** | 일찍 놓아줘야 뷰 절이 후반부를 가져간다 |
| style fidelity | 0.5 | Balanced 모드의 `threshold_a` |

🔴 **이게 3/4 재발의 새 위험원이다** — 정면 이미지를 물리는 행위 자체가 정면 구도를 끌어온다.
depth(0.45) + reference(0.45) + 뷰 네거티브 1.5 의 균형점을 실측으로 찾는다. 뷰가 틀어지면
**reference weight 를 먼저 내리고**(0.45 → 0.3), 그래도 안 되면 끈다(`-`).

### 3-4. 설정 초기값
| 항목 | 값 | 비고 |
|---|---|---|
| 해상도 | **832 × 1216** (인물 세로) | SDXL 은 총 화소가 1024² 근처여야 한다. 3뷰를 한 장에 욕심내지 않으므로 세로 인물 비율이 맞다 |
| Sampler | `Euler a` 또는 `DPM++ 2M SDE Karras` | |
| Steps | 28~32 | |
| CFG | **4~6** | Illustrious/NoobAI 계열은 CFG 를 낮게 쓴다. 7 이상이면 색이 타고 대비가 깨진다 |
| Hires fix | 1.5× · `4x-UltraSharp` 또는 `R-ESRGAN 4x+ Anime6B` · denoise 0.35 | 선택 |
| Seed | **정면이 통과하면 고정** | 측면·후면 생성 시 같은 시드 + `reference_only` |

## 4. 판정
`ImagePrompt_v4.txt` 의 체크리스트를 그대로 쓴다(색 4항목 최우선 → 형태 언어 4항목 → 레퍼런스 분담 → 비례 → 일관성).
**세 뷰 일치 항목은 3장을 나란히 놓고** 부위별로 대조한다.

## 5. 이 문서가 안 하는 것
모델 다운로드(사용자 계정) · Forge 재시작(사용자) · 게임/에디터 기동. `--api` 는 켜 뒀으나 **재시작해야 적용된다.**
